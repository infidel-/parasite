package particles;

// transient light pulse metadata emitted by particles
typedef _ParticleLightPulse = {
  var x: Float;
  var y: Float;
  var radiusTiles: Float;
  var intensity: Float;
  var tintR: Int;
  var tintG: Int;
  var tintB: Int;
  var durationMs: Float;
}
